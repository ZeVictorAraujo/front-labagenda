import Login from "../../components/Login/Login";

export default function LoginPage() {
    return (
        <div className="bg-blue-800 flex justify-center items-center w-screen h-screen">
            <Login />
        </div>
    );
}